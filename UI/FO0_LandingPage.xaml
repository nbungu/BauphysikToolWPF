<UserControl x:Class="BauphysikToolWPF.UI.FO0_LandingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BauphysikToolWPF.UI"
             xmlns:vm="clr-namespace:BauphysikToolWPF.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200">

    <UserControl.DataContext>
        <!-- globally sets a reference for Bindings in this xaml-->
        <vm:FO0_ViewModel/> 
    </UserControl.DataContext>

    <UserControl.Resources>

        <!--Custom ButtonGridPanel: light rounded corners, click effects-->
        <Style x:Key="ButtonStyleGridPanel" TargetType="{x:Type Button}">
            <EventSetter Event="MouseRightButtonUp" Handler="Button_MouseRightButtonUp"/>
            <Setter Property="Content" Value="{Binding ElementId}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <!--Element appearance with behaviour of a 'Button'-->
                        <Border x:Name="btn_border" CornerRadius="16" Background="{TemplateBinding Background}">
                            <Grid Margin="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height=".2*"/>
                                    <RowDefinition Height=".8*"/>
                                </Grid.RowDefinitions>
                                <Label Grid.Row="0" Content="{TemplateBinding Content}" ContentStringFormat='ID: {0}' HorizontalAlignment="Right" Foreground="White" FontSize="10"/>
                                <Image Grid.Row="1" ClipToBounds="True" SnapsToDevicePixels="True" Source="{Binding ElementImage}" Margin="8"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>            
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX=".96" ScaleY=".96" />
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!--Here: Binding (DataContext) is typeof 'Element' set by ItemsSource Property-->
        <DataTemplate x:Key="WrapPanelItem_DesignTemplate">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" x:Name="elementPanel_Button" Content="{Binding ElementId}" Style="{StaticResource ButtonStyleGridPanel}" Click="elementPanel_Button_Click"/>
                <Label Grid.Row="1" x:Name="recentElement_Label" Content="{Binding Name}" FontSize="14" HorizontalAlignment="Center" Margin="0,4,0,0"/>
            </Grid>
        </DataTemplate>

        <ItemsPanelTemplate x:Key="WrapPanel_Template">
            <WrapPanel x:Name="element_WrapPanel" ItemWidth="144" ItemHeight="192" ClipToBounds="True"/>
        </ItemsPanelTemplate>

    </UserControl.Resources>

    <Grid Background="{StaticResource PrimaryLightBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="56" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".2*" MinWidth="170" MaxWidth="290"/>
            <ColumnDefinition Width=".8*" MinWidth="320"/>
        </Grid.ColumnDefinitions>

        <Rectangle Grid.ColumnSpan="2" Fill="{StaticResource PrimaryDarkBrush}"/>
        <Label Grid.ColumnSpan="2" Content="Bauteilkatalog" FontSize="24" FontWeight="Bold" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Foreground="White"/>
        <Image Grid.Column="1" Grid.RowSpan="2" Width="80" Height="80" Source="../Ordner_Icon.ico" Margin="16" HorizontalAlignment="Right" VerticalAlignment="Top"/>
        
        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Vertical" Margin="16">
            <Button x:Name="createNewElement_Button" Content="Bauteil erstellen" Style="{StaticResource ButtonStyle02_A}" Click="createNewElement_Button_Click" Margin="0,0,0,16"/>
            <Button Content="Öffnen" Style="{StaticResource ButtonStyle02_B}" Margin="0,0,0,16"/>
            <Line X2="300" Stroke="Gray" HorizontalAlignment="Center"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Vertical" Margin="16" VerticalAlignment="Bottom">
            <Button x:Name="closeApp_Button" Content="Schließen" Style="{StaticResource ButtonStyle02_B}" Click="closeApp_Button_Click"/>
        </StackPanel>

        <Grid Grid.Row="1" Grid.Column="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width=".15*"/>
                <ColumnDefinition Width=".7*"/>
                <ColumnDefinition Width=".15*"/>
                <ColumnDefinition MinWidth="170" MaxWidth="290"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="1" Content="Zuletzt erstellte Bauteile" FontSize="14" FontWeight="Bold"/>

            <!--"WrapPanel inside a ListBox does pick up the DataTemplate you define .. so you have complete liberty to bind any list to your WrapPanel"-->
            <ItemsControl x:Name="element_ItemsControl"
                          Grid.Row="1"
                          Grid.Column="1"
                          ItemsSource="{Binding Elements}"
                          ItemsPanel="{StaticResource WrapPanel_Template}"
                          ItemTemplate="{StaticResource WrapPanelItem_DesignTemplate}"
                          Background="Transparent"                          
                          BorderThickness="0">
                <ItemsControl.Resources>
                    <ContextMenu x:Key="WrapPanel_ContextMenu" x:Name="WrapPanel_ContextMenu" Style="{StaticResource ContextMenuStyle01}" ItemContainerStyle="{StaticResource ContextMenuItemStyle01}">
                        <MenuItem x:Name="edit_MenuItem" Header="Bearbeiten" Click="edit_MenuItem_Click" >
                            <MenuItem.Icon>
                                <Image Source="{StaticResource ButtonIcon_Edit}" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem x:Name="delete_MenuItem" Header="Löschen" Click="delete_MenuItem_Click">
                            <MenuItem.Icon>
                                <Image Source="{StaticResource ButtonIcon_Cancel}" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem x:Name="lock_MenuItem" Header="Sperren" Click="lock_MenuItem_Click">
                            <MenuItem.Icon>
                                <Image Source="{StaticResource ButtonIcon_Lock}"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </ItemsControl.Resources>
            </ItemsControl>

            <Label Grid.Row="0" Grid.Column="3" Content="Projektdaten" FontSize="14" FontWeight="Bold"/>
            <Grid Grid.Row="1" Grid.Column="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width=".5*" MinWidth="64"/>
                    <ColumnDefinition Width=".5*" MinWidth="64"/>
                </Grid.ColumnDefinitions>

                <Label Grid.ColumnSpan="2" Content="Projektname:" FontSize="14"/>
                <TextBox Grid.Row="1" Grid.ColumnSpan="2" x:Name="projectName_TextBox" Text="{Binding ProjectName}" VerticalContentAlignment="Center" FontSize="14" Height="32" Margin="0,4,0,0"/>

                <Label Grid.Row="2" Grid.ColumnSpan="2" Content="Bearbeiter:" FontSize="14" Margin="0,8,0,0"/>
                <TextBox Grid.Row="3" Grid.ColumnSpan="2" x:Name="projectUser_TextBox" Text="{Binding ProjectUserName}" VerticalContentAlignment="Center" FontSize="14" Height="32" Margin="0,4,0,0"/>

                <Label Grid.Row="4" Grid.ColumnSpan="2" Content="Nutzungsart des Gebäudes:" FontSize="14" Margin="0,8,0,0"/>
                <RadioButton Grid.Row="5" Grid.Column="0" x:Name="selectUsage1_Button" GroupName="BuildingUsage" Content="Wohngebäude" Style="{StaticResource RadioButton01_A}" DataContext="{StaticResource ButtonIcon_OK}" Margin="0,4,4,0" Click="selectUsage1_Button_Click"/>
                <RadioButton Grid.Row="5" Grid.Column="1" x:Name="selectUsage0_Button" GroupName="BuildingUsage" Content="Nichtwohngebäude" Style="{StaticResource RadioButton01_A}" DataContext="{StaticResource ButtonIcon_OK}" Margin="4,4,0,0" Click="selectUsage0_Button_Click"/>

                <Label Grid.Row="6" Grid.ColumnSpan="2" Content="Alter des Gebäudes:" FontSize="14" Margin="0,8,0,0"/>
                <RadioButton Grid.Row="7" Grid.Column="0" x:Name="selectAge1_Button" GroupName="BuildingAge" Content="Neubau" Style="{StaticResource RadioButton01_A}" DataContext="{StaticResource ButtonIcon_OK}" Margin="0,4,4,0" Click="selectAge1_Button_Click"/>
                <RadioButton Grid.Row="7" Grid.Column="1" x:Name="selectAge0_Button" GroupName="BuildingAge" Content="Bestandsgebäude" Style="{StaticResource RadioButton01_A}" DataContext="{StaticResource ButtonIcon_OK}" Margin="4,4,0,0" Click="selectAge0_Button_Click"/>
            </Grid>           
            
        </Grid>
    </Grid>
</UserControl>
