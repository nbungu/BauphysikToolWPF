<Window x:Class="BauphysikToolWPF.UI.NewElementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BauphysikToolWPF.UI"
        mc:Ignorable="d"
        Title="NewElementWindow" Height="200" Width="320" WindowStartupLocation="CenterScreen">

    <Grid Background="{StaticResource PrimaryLightBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height=".5*"/>
            <RowDefinition Height=".5*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Margin="8">
            <Label Content="Name des Bauteils" FontSize="14"/>
            <TextBox x:Name="elementName_TextBox" Text="" VerticalContentAlignment="Center" FontSize="14" MaxLength="50" Height="32" HorizontalAlignment="Stretch"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="8">
            <Button x:Name="createElement_Button" Content="Übernehmen" Click="createElement_Button_Click" Style="{StaticResource ButtonStyle02_A}"/>
        </Grid>

    </Grid>
</Window>
