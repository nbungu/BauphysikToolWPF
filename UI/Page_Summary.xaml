<UserControl x:Class="BauphysikToolWPF.UI.Page_Summary"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="clr-namespace:BauphysikToolWPF.Services.UI"
             xmlns:controls="clr-namespace:BauphysikToolWPF.UI.CustomControls"
             xmlns:glWpfControl="clr-namespace:OpenTK.Wpf;assembly=GLWpfControl"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1200">

    <!--<UserControl.DataContext>
        <vm:Page_Summary_VM/>
    </UserControl.DataContext>-->

    <!--
    the DataContext is used to specify the data source for a UI element,
    while the ItemsSource is used to specify the data source for a list of items.
    Both can be used for binding to display and modify data in the UI.
    -->

    <!--Main Grid: 3x2-->
    <Grid Background="{StaticResource PrimaryLightBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="320"/>
            <ColumnDefinition Width="360" MaxWidth="360" MinWidth="16"/>
        </Grid.ColumnDefinitions>

        <!--Top -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" Background="{StaticResource PrimaryMediumBrush}">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Style="{StaticResource ButtonMiniTransparent}" Command="{Binding SwitchPageCommand}" CommandParameter="{x:Static ui:NavigationPage.LayerSetup}" Tag="{StaticResource ButtonIcon_Prev_B}" Margin="4"/>
                    <Label Content="Zurück"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Ellipse Fill="{Binding SelectedElementColorCode}" Width="16" Height="16" Margin="0,0,8,0"/>
                    <Label Content="{Binding Title}" FontSize="18" FontWeight="Bold" FontStretch="Expanded"/>
                    <Label Content="{Binding SelectedElementConstructionName}" ContentStringFormat="({0})" FontSize="18" FontStyle="Italic" Margin="0,0,8,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Label Content="Weiter"/>
                    <Button Style="{StaticResource ButtonMiniTransparent}" Command="{Binding SwitchPageCommand}" CommandParameter="{x:Static ui:NavigationPage.TemperatureCurve}" Tag="{StaticResource ButtonIcon_Next_B}" Margin="4"/>
                </StackPanel>
            </Grid>
        </Border>

        <!--Cross Section Image-->
        <Grid Grid.Column="0" Grid.Row="1" Margin="16" ClipToBounds="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="1" Content="Innen" Visibility="{Binding CrossSectionLabelsVisibility}" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
            <Label Grid.Row="2" Grid.Column="1" Content="Außen" Visibility="{Binding CrossSectionLabelsVisibility}" VerticalAlignment="Top" HorizontalAlignment="Center"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Innen" Visibility="{Binding VerticalCutLabelsVisibility}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            <Label Grid.Row="1" Grid.Column="2" Content="Außen" Visibility="{Binding VerticalCutLabelsVisibility}" VerticalAlignment="Center" HorizontalAlignment="Left"/>

            <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" >
                <RadioButton IsChecked="{Binding IsCrossSectionChecked}" GroupName="View" Content="Querschnitt" Style="{StaticResource RadioButton01_B}" Margin="0,4,4,0"/>
                <RadioButton IsChecked="{Binding IsVerticalCutChecked}" GroupName="View" Content="Vertikalschnitt" Style="{StaticResource RadioButton01_B}" Margin="4,4,0,0"/>
            </StackPanel>

            <Label Grid.Row="1" Grid.Column="1" Visibility="{Binding NoLayersVisibility}" Content="Keine Abbildung verfügbar" Foreground="Gray" FontStyle="Italic" VerticalAlignment="Center" HorizontalAlignment="Center"/>

            <Grid Grid.Row="1" Grid.Column="1" Margin="8">
                <glWpfControl:GLWpfControl x:Name="OpenTkControl"/>
            </Grid>

        </Grid>

        <!--Grid Splitter-->
        <controls:ExpandableGridSplitter Grid.Row="1" Grid.Column="1"
                                         HorizontalAlignment="Left"
                                         VerticalAlignment="Stretch"
                                         Width="22"
                                         Background="Transparent"
                                         Margin="-11,16,0,16"/>

        <Grid Grid.Row="1" Grid.Column="1" Margin="16">
            <ScrollViewer VerticalScrollBarVisibility="Hidden" CanContentScroll="False" PreviewMouseWheel="UIElement_OnPreviewMouseWheel">
                <StackPanel>
                    <controls:Gauge Title="U-Wert:"
                                    Caption="{Binding UValueGauge.Caption}"
                                    ToolTip="{Binding UValueGauge.Tooltip}"
                                    Value="{Binding UValueGauge.Value}"
                                    UnitLabel="{Binding UValueGauge.Unit}"
                                    MinValue="{Binding UValueGauge.ScaleMin}"
                                    MaxValue="{Binding UValueGauge.ScaleMax}"
                                    SymbolLabelBase="{Binding UValueGauge.SymbolBase}"
                                    SymbolLabelSubscript="{Binding UValueGauge.SymbolSubscript}"
                                    ReferenceMarkerValue="{Binding UValueGauge.MarkerValue}"
                                    MarkerSymbolLabelBase="{Binding UValueGauge.MarkerSymbolBase}"
                                    MarkerSymbolLabelSubscript="{Binding UValueGauge.MarkerSymbolSubscript}"
                                    Margin="4,0,4,8"/>
                    <controls:Gauge Title="R-Wert:"
                                    Caption="{Binding RValueGauge.Caption}"
                                    ToolTip="{Binding RValueGauge.Tooltip}"
                                    Value="{Binding RValueGauge.Value}"
                                    UnitLabel="{Binding RValueGauge.Unit}"
                                    MinValue="{Binding RValueGauge.ScaleMin}"
                                    MaxValue="{Binding RValueGauge.ScaleMax}"
                                    SymbolLabelBase="{Binding RValueGauge.SymbolBase}"
                                    SymbolLabelSubscript="{Binding RValueGauge.SymbolSubscript}"
                                    ReferenceMarkerValue="{Binding RValueGauge.MarkerValue}"
                                    MarkerSymbolLabelBase="{Binding RValueGauge.MarkerSymbolBase}"
                                    MarkerSymbolLabelSubscript="{Binding RValueGauge.MarkerSymbolSubscript}"
                                    ReverseGradient="True"
                                    Margin="4,0,4,8"/>

                    <!--Eigenschaften-->
                    <Expander Header="Bauteildaten" IsExpanded="True" Margin="0,0,0,8">
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Disabled">
                            <controls:PropertyDataGrid x:Name="ElementPropertiesDataGrid" Properties="{Binding ElementProperties}"/>
                        </ScrollViewer>
                    </Expander>

                    <Expander Header="Gewählte Randbedingungen" IsExpanded="True" Margin="0,0,0,8">
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Disabled">
                            <controls:PropertyDataGrid x:Name="EnvironmentPropertiesDataGrid" Properties="{Binding EnvironmentProperties}"/>
                        </ScrollViewer>
                    </Expander>
                </StackPanel>
            </ScrollViewer>
        </Grid>

    </Grid>

</UserControl>
