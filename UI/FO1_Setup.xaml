<UserControl x:Class="BauphysikToolWPF.UI.FO1_Setup"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:BauphysikToolWPF.UI"  
            xmlns:vm="clr-namespace:BauphysikToolWPF.UI.ViewModels"
            mc:Ignorable="d"
            d:DesignHeight="700" d:DesignWidth="1200">

    <UserControl.DataContext>
        <!-- globally sets a reference for Bindings in this xaml-->
        <vm:FO1_ViewModel/>
    </UserControl.DataContext>
    
    <!--
    the DataContext is used to specify the data source for a UI element,
    while the ItemsSource is used to specify the data source for a list of items.
    Both can be used for binding to display and modify data in the UI.
    -->

    <UserControl.Resources>
        <!--
        Defines a Template for the Items in the LayerListView.
        The Bindings are retrieved from the ItemsSource which is set by the backend.
        -->
        <DataTemplate x:Key="LVItem_DesignTemplate">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="26" />
                    <RowDefinition Height="26" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="52"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.RowSpan="2">
                    <Ellipse Fill="{Binding Material.ColorCode}" Height="36" Width="36" Margin="0,0,8,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <Label Content="{Binding LayerPosition}" Margin="0,0,8,0" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
                </Grid>
                <Label Grid.Column="1" Content="{Binding Material.Name}" FontWeight="Bold" Height="32"/>
                <Label Grid.Column="1" Grid.Row="1" Content="{Binding Material.Category}" FontStyle="Italic" VerticalAlignment="Bottom" Height="32"/>
                <Label Grid.Column="2" Grid.RowSpan="2" Content="{Binding LayerThickness}" ContentStringFormat='d = {0} cm' VerticalAlignment="Center"/>
                <Label Grid.Column="3" Grid.RowSpan="2" Content="{Binding LayerResistance}" ContentStringFormat='Ri = {0} (m²K)/W' VerticalAlignment="Center"/>
                
                <!--TODO make button appear only if lvitem is clicked-->
                <Button Content="T" Grid.Column="4" Grid.RowSpan="2">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <!-- Default Content value -->
                            <Setter Property="Visibility" Value="Hidden"/>
                            <!-- Triggered values -->
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver, ElementName=layers_ListView}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

            </Grid>
        </DataTemplate>

        <Style x:Key="LVItem_Properties" TargetType="{x:Type ListViewItem}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="ContextMenu">
                <Setter.Value>
                    <ContextMenu>
                        <MenuItem Name="menuItemClose" Header="Exit"/>
                        <MenuItem Name="menuItemOpen" Header="Open"/>
                    </ContextMenu>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CustomButton" TargetType="{x:Type Button}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX=".96" ScaleY=".96" />
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <!--These setters are for the default appearance-->
            <Setter Property="Width" Value="40"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <EventSetter Event="PreviewTextInput" Handler="numericData_PreviewTextInput"/>
            <!--These setters are for the trigger appearance-->
            <!--
            <Style.Triggers>
                <DataTrigger Binding="{Binding Source={x:Reference Te_Category_Picker}, Path=Text}" Value="Custom Value">
                    <Setter Property="IsEnabled" Value="True"/>
                </DataTrigger>
            </Style.Triggers>
            -->
        </Style>

    </UserControl.Resources>

    <!--Main Grid: 2x2-->
    <Grid Background="#EDEDED">

        <Grid.RowDefinitions>
            <RowDefinition Height=".6*"/>
            <RowDefinition Height=".4*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".4*"/>
            <ColumnDefinition Width=".6*"/>
        </Grid.ColumnDefinitions>

        <!--Top Left Grid -->
        <Grid Grid.Row="0" Grid.Column="0" Margin="16">

            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Content="Aktueller Bauteilaufbau"/>

            <StackPanel Orientation="Horizontal" Grid.Row="1">

                <Button x:Name="addLayer_Button" Style="{StaticResource CustomButton}" Click="addLayerClicked" Margin="0,8,8,8">
                    <StackPanel Orientation="Horizontal" Margin="4,0,16,0">
                        <Image Source="/Resources/Icons/GL_010x80_create.png" Stretch="Fill" Height="36" Width="36" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <TextBlock Text="Add Layer" VerticalAlignment="Center" Margin="4,0,0,0"/>
                    </StackPanel>
                </Button>

                <Button x:Name="deleteLayer_Button" Style="{StaticResource CustomButton}" Click="deleteLayerClicked" Margin="0,8,8,8">
                    <StackPanel Orientation="Horizontal" Margin="4,0,16,0">
                        <Image Source="/Resources/Icons/GL_011x80_delete.png" Stretch="Fill" Height="30" Width="30" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <TextBlock Text="Delete Layer" VerticalAlignment="Center" Margin="4,0,0,0"/>
                    </StackPanel>
                </Button>

                <Button x:Name="deleteAllLayers_Button" Style="{StaticResource CustomButton}" Click="deleteAllLayersClicked" Margin="0,8,8,8">
                    <StackPanel Orientation="Horizontal" Margin="4,0,16,0">
                        <Image Source="/Resources/Icons/GL_011x80_delete.png" Stretch="Fill" Height="30" Width="30" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <TextBlock Text="Delete All" VerticalAlignment="Center" Margin="4,0,0,0"/>
                    </StackPanel>
                </Button>

                <Button x:Name="editLayer_Button" Style="{StaticResource CustomButton}" Click="editLayerClicked" Margin="0,8,8,8">
                    <StackPanel Orientation="Horizontal" Margin="4,0,16,0">
                        <Image Source="/Resources/Icons/GL_015x80_edit.png" Stretch="Fill" Height="30" Width="30" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <TextBlock Text="Edit Layer" VerticalAlignment="Center" Margin="4,0,0,0"/>
                    </StackPanel>
                </Button>

            </StackPanel>

            <ListView x:Name="layers_ListView"
                      Grid.Row="2"
                      ItemsSource="{Binding Layers, Mode=OneWay}"
                      ItemTemplate="{StaticResource LVItem_DesignTemplate}"
                      ItemContainerStyle="{StaticResource LVItem_Properties}"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch"
                      SelectionChanged="layers_ListView_SelectionChanged"
                      SelectionMode="Single"/>
            <!--ItemsSource gets updated by backend-->
        </Grid>

        <!--Top Right Grid -->
        <Grid Grid.Row="0" Grid.Column="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Label Content="Außen - Linke Seite" HorizontalContentAlignment="Left"/>
            <Label x:Name="layer_Label" Content="-" HorizontalContentAlignment="Center" />
            <Label Content="Innen - Rechte Seite" HorizontalContentAlignment="Right" />

            <Viewbox Grid.Row="1" ClipToBounds="True">
                <!--Canvas inside of ViewBox makes it dynamically sizable-->
                <Canvas x:Name="layers_Canvas" Width="400" Height="200" SnapsToDevicePixels="True"/>
                <!-- Canvas Children defined in backend-->
            </Viewbox>

        </Grid>

        <!--Bottom Right Grid -->
        <Grid Grid.Row="1" Grid.Column="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="40" />
                <RowDefinition Height="40" />
                <RowDefinition Height="40" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Content="Außenseitige Randbedingungen" FontWeight="Bold" Grid.Column="0"/>
            <Label Content="Innenseitige Randbedingungen" FontWeight="Bold" Grid.Column="2"/>

            <!--Außenseitige Randbedingungen-->

            <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0" Margin="0,8,8,8">
                <Label Content="Air Temperature: " Width="100" Height="32" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
                <ComboBox x:Name="Te_Category_Picker" ItemsSource="{Binding Te_Keys}" Width="160" VerticalAlignment="Stretch" SelectionChanged="Te_Category_Picker_SelectionChanged"/>
                <TextBox x:Name="Te_Input" Style="{StaticResource TextBoxStyle}"/>
                <Label Content="°C" Height="32" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0" Margin="0,8,8,8">
                <Label Content="Rse: " Width="100" Height="32" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
                <ComboBox x:Name="Rse_Category_Picker" ItemsSource="{Binding Rse_Keys}" Width="160" VerticalAlignment="Stretch" SelectionChanged="Rse_Category_Picker_SelectionChanged"/>
                <TextBox x:Name="Rse_Input" Style="{StaticResource TextBoxStyle}"/>
                <Label Content="(m²K)/W" Height="32" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.Column="0" Margin="0,8,8,8">
                <Label Content="Rel. Humitdity: " Width="100" Height="32" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
                <ComboBox x:Name="RelFe_Category_Picker" ItemsSource="{Binding Rel_Fe_Keys}" Width="160" VerticalAlignment="Stretch" SelectionChanged="RelFe_Category_Picker_SelectionChanged"/>
                <TextBox x:Name="RelFe_Input" Style="{StaticResource TextBoxStyle}"/>
                <Label Content="%" Height="32" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
            </StackPanel>

            <!--Mittlere Trennlinie-->
            <Line Grid.RowSpan="4" Grid.Column="1" Y2="600" VerticalAlignment="Stretch" HorizontalAlignment="Center" Stroke="SlateGray" StrokeThickness="2"/>

            <!--Innenseitige Randbedingungen-->
            <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="2" Margin="0,8,8,8">
                <Label Content="Air Temperature: " Width="100" Height="32" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
                <ComboBox x:Name="Ti_Category_Picker" ItemsSource="{Binding Ti_Keys}" Width="160" VerticalAlignment="Stretch" SelectionChanged="Ti_Category_Picker_SelectionChanged"/>
                <TextBox x:Name="Ti_Input" Style="{StaticResource TextBoxStyle}"/>
                <Label Content="°C" Height="32" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="2" Margin="0,8,8,8">
                <Label Content="Rsi: " Width="100" Height="32" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
                <ComboBox x:Name="Rsi_Category_Picker" ItemsSource="{Binding Rsi_Keys}" Width="160" VerticalAlignment="Stretch" SelectionChanged="Rsi_Category_Picker_SelectionChanged"/>
                <TextBox x:Name="Rsi_Input" Style="{StaticResource TextBoxStyle}"/>
                <Label Content="(m²K)/W" Height="32" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.Column="2" Margin="0,8,8,8">
                <Label Content="Rel. Humitdity: " Width="100" Height="32" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
                <ComboBox x:Name="RelFi_Category_Picker" ItemsSource="{Binding Rel_Fi_Keys}" Width="160" VerticalAlignment="Stretch" SelectionChanged="RelFi_Category_Picker_SelectionChanged"/>
                <TextBox x:Name="RelFi_Input" Style="{StaticResource TextBoxStyle}"/>
                <Label Content="%" Height="32" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
            </StackPanel>
        </Grid>

        <GridSplitter Grid.Column="1" Grid.RowSpan="2" HorizontalAlignment="Left" Margin="-1,16,0,16" VerticalAlignment="Stretch" Width="2" Background="SlateGray"/>

    </Grid>

</UserControl>
