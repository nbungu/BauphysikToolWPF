<Window x:Class="BauphysikToolWPF.UI.CustomControls.SaveExitConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SaveExitConfirmationDialog"
        WindowStyle="None"
        Height="320"
        Width="560">

    <Grid Margin="16">

        <StackPanel>
            <TextBlock Text="Änderungen speichern?" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
            <TextBlock Text="Sie schließen dieses Projekt. Alle ungespeicherten Änderungen gehen verloren. Wollen Sie speichern, bevor Sie das Programm schließen?"
                       TextWrapping="Wrap"
                       FontSize="14"/>
        </StackPanel>
        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" >
            <Button Width="80" Style="{StaticResource ButtonHighlighted}" HorizontalContentAlignment="Center" IsDefault="True" Content="Ja" Click="Yes_Click" Margin="0,0,8,0"/>
            <Button Width="80" Style="{StaticResource ButtonStandard}" HorizontalContentAlignment="Center" Content="Nein" Click="No_Click" Margin="0,0,8,0"/>
            <Button Width="180" Style="{StaticResource ButtonStandard}" HorizontalContentAlignment="Center" IsCancel="True" Content="Abbrechen und zurück" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
